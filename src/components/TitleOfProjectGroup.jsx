
const TitleOfProjectGroup = ({title, textColor}) => {
  return (
      <div className='mt-5'>
          <h2 className={`text2 ${textColor} text-2xl`}>{title}</h2>
      </div>
  )
}

export default TitleOfProjectGroup
